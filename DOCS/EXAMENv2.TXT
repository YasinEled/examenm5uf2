$  git init

$  git remote add origin https://github.com/YasinEled/examenm5uf2.git

$  git status -s

$ git add .

$ git status -s

$ git commit -m "primer commit per pujar al github"

$ git push --set-upstream origin master

$ git clone https://github.com/YasinEled/examenm5uf2.git examenm5uf2

$ git status -s

$ git add .

$ git commit -m "Pujada Versio 2"

$ git push

$ git pull (repositorio Yasin para actualizar el git)

$ git status -s

$ git add .

$ git commit -m "Pujada Versio 3"

$ git push

$ git pull (repositorio Coba para actualizar el git)

$ git status -s

$ git add .

$ git commit -m "Pujada Versio 3 - continuació"

$ git push

$ git status -s

$ git add .

$ git commit -m "Pujada Versio 3 - continuación sergio"

$ git push

